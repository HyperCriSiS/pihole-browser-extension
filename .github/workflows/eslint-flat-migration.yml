name: Build flat lint migration

on:
  push:
    branches:
      - chore/eslint-flat-config
  pull_request:
    branches:
      - master

permissions:
  contents: write

jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out migration branch
        uses: actions/checkout@v6
        with:
          ref: chore/eslint-flat-config

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          package-manager-cache: false

      - name: Regenerate lockfile
        shell: bash
        run: |
          set +e
          rm -f package-lock.json .github/npm-install-error.txt
          npm install --package-lock-only --ignore-scripts --no-audit --no-fund > .github/npm-install-error.txt 2>&1
          status=$?
          cat .github/npm-install-error.txt
          if [ "$status" -ne 0 ]; then
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git add -A package-lock.json .github/npm-install-error.txt
            git commit -m 'Record npm dependency resolution failure'
            git push origin HEAD:chore/eslint-flat-config
            exit "$status"
          fi
          rm -f .github/npm-install-error.txt

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Apply formatting and safe lint fixes
        shell: bash
        run: |
          set +e
          rm -f .github/lint-migration-error.txt

          npm run prettier-format > .github/lint-migration-error.txt 2>&1
          status=$?
          if [ "$status" -eq 0 ]; then
            npm run eslint -- --fix >> .github/lint-migration-error.txt 2>&1
            status=$?
          fi
          if [ "$status" -eq 0 ]; then
            npm run prettier-format >> .github/lint-migration-error.txt 2>&1
            status=$?
          fi

          cat .github/lint-migration-error.txt
          if [ "$status" -ne 0 ]; then
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git add .github/lint-migration-error.txt
            git commit -m 'Record lint migration failure'
            git push origin HEAD:chore/eslint-flat-config
            exit "$status"
          fi
          rm -f .github/lint-migration-error.txt

      - name: Run ESLint
        run: npm run eslint

      - name: Run Prettier check
        run: npm run prettier-check

      - name: Build Chrome and Firefox packages
        run: npm run build

      - name: Commit migration result
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/eslint-flat-migration.yml .github/eslint-flat-trigger .github/npm-install-error.txt .github/lint-migration-error.txt
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Migrate linting to ESLint flat config'
          git push origin HEAD:chore/eslint-flat-config
